# Day 56 – Rendering Templates with Flask

## 📚 What I Learned

Today I learned how to:

- Use `render_template()` in Flask
- Separate HTML from Python
- Use `templates/` folder correctly
- Use `static/` folder for CSS and assets
- Build structured web applications
- Pass variables from Python to HTML

This marks the transition from inline HTML → proper web architecture.

---

## 📂 Project Structure

Day_56_rendering_with_Flask/
│
├── first_project/
│ ├── server.py
│ ├── templates/
│ └── static/
│── README.md
└──html5up-identity